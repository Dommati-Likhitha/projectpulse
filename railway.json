{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn ProjectPulse.wsgi:application --bind 0.0.0.0:$PORT"
  },
  "release": {
    "command": "python manage.py migrate --noinput && python manage.py collectstatic --noinput"
  }
}